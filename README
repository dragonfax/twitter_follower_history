A quick script to track my followers on twitter on a daily basis. 

uses a git repo to store the follower history for versioning.

uses the python based twitter api available at https://github.com/sixohsix/twitter

simply
1. use "easy_install twitter" to install the api.
2. then run "twitter" once to setup oauth


